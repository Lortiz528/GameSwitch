
// import NewAccount from "../../Components/Accounts/Signup/NewAccount";

const New = ({ handleUser, users }) => {
  return <NewAccount handleUser={handleUser} users={users} />;
};

export default New;